# AGENTS.md — Lawyer Website (Bulgarian)

## Project

Static website for a practicing lawyer. Entire site is in Bulgarian.

## Docs-first workflow

- All planning / specs go in `docs/` as Markdown.
- Start with a PRD in `docs/prd.md` before any code.

## Stack

To be decided. No frameworks or build tools have been chosen yet. When one is:
- commit lockfiles and pin exact versions
- prefer static-site-friendly tooling (e.g. plain HTML/CSS/JS, Astro, 11ty, etc.)
- set up lint + format before writing real pages

## Conventions

- All user-facing text is in Bulgarian only.
- Keep the site static (no backend, no database).
